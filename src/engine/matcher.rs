//! Closed `Matcher` enum and supporting types.
//!
//! Populated in commit C2. Empty in C1 (skeleton only) so the module tree
//! compiles without forcing the rest of the refactor to land in one step.
